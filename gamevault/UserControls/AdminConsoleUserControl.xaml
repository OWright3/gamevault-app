<UserControl x:Class="gamevault.UserControls.AdminConsoleUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:conv="clr-namespace:gamevault.Converter"
             xmlns:local="clr-namespace:gamevault.UserControls"            
             xmlns:vm="clr-namespace:gamevault.ViewModels"            
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Loaded="UserControl_Loaded" KeyDown="Reload_Click" Focusable="True" IsVisibleChanged="UserControl_IsVisibleChanged">
    <UserControl.Resources>
        <conv:InverseEmptyStringConverter x:Key="InvertStringConv"/>
        <conv:InverseNullConverter x:Key="InvertNullConv"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="{DynamicResource MahApps.Brushes.ThemeBackground2}" Grid.Row="0">
            <TextBlock Text="Admin Console" FontSize="35" Margin="20,0,0,0" Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}"/>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid MaxWidth="1200" Margin="20" HorizontalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{DynamicResource MahApps.Brushes.ThemeBackground2}" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Server Information" FontSize="20" FontWeight="Bold"/>

                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
                            <TextBlock Text="{Binding Path=ServerVersionInfo.Key}" FontSize="14" VerticalAlignment="Center"/>
                            <TextBlock VerticalAlignment="Center" FontSize="14" Margin="8,0,0,0">
                                <TextBlock.Style>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=ServerVersionInfo.Value,Converter={StaticResource InvertStringConv}}" Value="False">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                                <Hyperlink TextDecorations="None" Foreground="Red" NavigateUri="{Binding Path=ServerVersionInfo.Value}" RequestNavigate="ServerUpdate_Navigate">
                                    <Hyperlink.Inlines>
                                        <Run Text="(Update Available)"/>
                                    </Hyperlink.Inlines>
                                </Hyperlink>
                            </TextBlock>
                        </StackPanel>

                        <TextBlock Grid.Row="2" Text="{Binding Source={x:Static vm:SettingsViewModel.Instance},Path=ServerUrl,StringFormat='{}Server Address: {0}'}" FontSize="14" Margin="0,4,0,0"/>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Background="{DynamicResource MahApps.Brushes.ThemeBackground2}" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <local:IconButton Text="Backup / Restore Database" Icon="{StaticResource IconDatabase}" IconScale="0.8" CornerRadius="6" FontSize="14" Width="220" Height="36" Click="BackupRestore_Click"/>
                            <local:IconButton Text="Reindex Games" Icon="{StaticResource IconReload}" IconScale="0.8" CornerRadius="6" FontSize="14" Width="140" Height="36" Margin="10,0,0,0" Click="Reindex_Click"/>
                            <mah:ToggleSwitch Header="Show deleted Users" IsOn="{Binding ShowDeletedUsers,Mode=TwoWay}" VerticalAlignment="Center" Margin="20,0,0,0"/>
                        </StackPanel>

                        <Button x:Name="uiBtnReload" Grid.Column="1" Style="{StaticResource ButtonWrapper}" Cursor="Hand" Width="36" Height="36" ToolTip="Refresh admin console (F5)" Click="Reload_Click">
                            <Grid Style="{DynamicResource HoverEffect}" Background="Transparent" RenderTransformOrigin="0.5,0.5">
                                <Path Data="{StaticResource IconReload}" Fill="{DynamicResource MahApps.Brushes.ThemeForeground}" Margin="5"/>
                                <Grid.RenderTransform>
                                    <ScaleTransform/>
                                </Grid.RenderTransform>
                            </Grid>
                        </Button>
                    </Grid>
                </Border>

                <Border Grid.Row="2" Background="{DynamicResource MahApps.Brushes.ThemeBackground2}" CornerRadius="8" Padding="15">
                    <DockPanel LastChildFill="True">
                        <Grid DockPanel.Dock="Top" Margin="0,0,0,10">
                            <TextBlock Text="Users" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                            <Button Style="{StaticResource ButtonWrapper}" HorizontalAlignment="Right" Cursor="Hand" Width="36" Height="36" Click="RegistrateUser_Click" ToolTip="Registrate new User">
                                <Grid Style="{DynamicResource HoverEffect}" Background="Transparent" RenderTransformOrigin="0.5,0.5">
                                    <Path Data="{StaticResource IconAddSimple}" Fill="{DynamicResource MahApps.Brushes.ThemeForeground}" Stroke="{DynamicResource MahApps.Brushes.ThemeForeground}" Margin="5"/>
                                    <Grid.RenderTransform>
                                        <ScaleTransform/>
                                    </Grid.RenderTransform>
                                </Grid>
                            </Button>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding Path=Users}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Height="82" Margin="0,0,0,8" CornerRadius="6" Padding="8">
                                        <Border.Style>
                                            <Style TargetType="{x:Type Border}">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Setter Property="Background" Value="#26FF0000"/>
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding Path=DataContext.ShowDeletedUsers,RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False" />
                                                            <Condition Binding="{Binding Path=DeletedAt,Converter={StaticResource InvertNullConv}}" Value="False" />
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </MultiDataTrigger>
                                                    <DataTrigger Binding="{Binding Path=DeletedAt}" Value="{x:Null}">
                                                        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ThemeBackground}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="62"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="145"/>
                                                <ColumnDefinition Width="190"/>
                                                <ColumnDefinition Width="80"/>
                                            </Grid.ColumnDefinitions>

                                            <Button Grid.Column="0" Style="{StaticResource ButtonWrapper}" Width="52" Height="52" HorizontalAlignment="Left" VerticalAlignment="Center" Cursor="Hand" Click="ShowUser_Click">
                                                <local:CacheImage ImageCacheType="UserAvatar" Data="{Binding}" Stretch="Fill" CornerRadius="4"/>
                                            </Button>

                                            <TextBlock Grid.Column="1" FontSize="16" Margin="10,0,10,0" Text="{Binding Path=Username}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>

                                            <mah:ToggleSwitch Grid.Column="2" Header="Activated" IsOn="{Binding Path=Activated}" Toggled="Activated_Toggled" VerticalAlignment="Center"/>

                                            <ComboBox Grid.Column="3" ItemsSource="{Binding Path=DataContext.PermissionRoleEnumTypes,RelativeSource={RelativeSource AncestorType=UserControl}}" SelectedItem="{Binding Path=Role}" SelectionChanged="PermissionRole_SelectionChanged" VerticalAlignment="Center" Margin="10,0"/>

                                            <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                                <Button Style="{StaticResource ButtonWrapper}" Cursor="Hand" Width="32" Height="32" Click="EditUser_Clicked" ToolTip="Edit user">
                                                    <Grid Style="{DynamicResource HoverEffect}" Background="Transparent" RenderTransformOrigin="0.5,0.5">
                                                        <Path Data="{StaticResource IconInstalledGamesSettings}" Fill="{DynamicResource MahApps.Brushes.ThemeForeground}" Margin="5"/>
                                                        <Grid.RenderTransform>
                                                            <ScaleTransform/>
                                                        </Grid.RenderTransform>
                                                    </Grid>
                                                </Button>
                                                <Button Style="{StaticResource ButtonWrapper}" Cursor="Hand" Width="32" Height="32" Margin="8,0,0,0" Click="DeleteUser_Clicked">
                                                    <Grid Style="{DynamicResource HoverEffect}" Background="Transparent" RenderTransformOrigin="0.5,0.5">
                                                        <Path Margin="5">
                                                            <Path.Style>
                                                                <Style TargetType="{x:Type Path}">
                                                                    <Setter Property="Data" Value="{DynamicResource IconReload}"/>
                                                                    <Setter Property="Fill" Value="{DynamicResource MahApps.Brushes.ThemeForeground}"/>
                                                                    <Setter Property="ToolTip" Value="Recover user"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Path=DeletedAt}" Value="{x:Null}">
                                                                            <Setter Property="Data" Value="{DynamicResource IconTrash}"/>
                                                                            <Setter Property="Fill" Value="IndianRed"/>
                                                                            <Setter Property="ToolTip" Value="Delete user"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Path.Style>
                                                        </Path>
                                                        <Grid.RenderTransform>
                                                            <ScaleTransform/>
                                                        </Grid.RenderTransform>
                                                    </Grid>
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </DockPanel>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
